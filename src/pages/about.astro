---
import BaseLayout from '../layouts/BaseLayout.astro';
const pageTitle = "About Me";

const identity = {
	firstName: "S1oop",
	country: "the U.S.A.",
	occupation: "Software Engineer",
	hobbies: ["bike riding", "sports", "reading", "birds and sailboats"],
};

const skills = ["C#", "SQL", "HTML 5", "CSS 3", "JS ES6", "Node", "nHibernate", "Razor", "Postman", "VS / VS Code", "SSMS", "MVC", "EF"];

const happy = true;
const finished = false;
const goal = 3;
---
<BaseLayout pageTitle={pageTitle}>
	<p>Here are a few facts about me:</p>
		<ul>
		<li>My name is {identity.firstName}.</li>
		<li>I live in {identity.country} and I work as a {identity.occupation}.</li>
		{identity.hobbies.length >= 2 &&
			<li>Some of my hobbies are: {identity.hobbies[0]},  {identity.hobbies[1]}, {identity.hobbies[2]}, {identity.hobbies[3]}</li>
		}
		</ul>
		<p>My skills are:</p>
		<ul>
		{skills.map((skill) => <li class="skill">{skill}</li>)}
		</ul>

		{happy && <p>I am happy to be learning Astro!</p>}

		{finished && <p>I finished this tutorial!</p>}

		{goal === 3 ? <p>My goal is to finish in 3 days.</p> : <p>My goal is not 3 days.</p>}
</BaseLayout>
